{
  "name": "coffeechat",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ coffeechat",
    "create-local": "graph create --node http://localhost:8020/ coffeechat",
    "remove-local": "graph remove --node http://localhost:8020/ coffeechat",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 coffeechat",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.27.0",
    "matchstick-as": "0.5.0"
  }
}
