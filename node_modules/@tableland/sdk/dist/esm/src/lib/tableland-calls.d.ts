import { StructureHashResult, ReadQueryResult, WriteQueryResult, ReceiptResult, Connection } from "./connection.js";
import { list } from "./list.js";
export interface RpcParams {
    controller?: string;
    create_statement?: string;
    statement?: string;
    txn_hash?: string;
}
export interface RpcReceipt<T = any> {
    jsonrpc: string;
    id: number;
    result: T;
}
declare function hash(this: Connection, query: string): Promise<StructureHashResult>;
declare function read(this: Connection, query: string): Promise<ReadQueryResult>;
declare function write(this: Connection, query: string): Promise<WriteQueryResult>;
declare function receipt(this: Connection, txnHash: string): Promise<ReceiptResult | undefined>;
export { hash, list, receipt, read, write };
